# Example Ollama systemd override
# Path on host: /etc/systemd/system/ollama.service.d/override.conf
# Purpose: allow Docker containers to reach the host-level Ollama service through the Docker host gateway.

[Service]
Environment="OLLAMA_HOST=0.0.0.0:11434"
